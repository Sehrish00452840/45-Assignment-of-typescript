"use strict";
// Define a function 
function show_magicians(magicians) {
    magicians.forEach(name => console.log(name));
}
// Define an array
let magician_names = ["John", "Tonnny", "Claire", "Micheal"];
// Call the show_magicians function
show_magicians(magician_names);
